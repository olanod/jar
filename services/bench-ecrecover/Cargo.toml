[package]
name = "bench-ecrecover"
version = "0.1.0"
edition = "2021"

[dependencies]
k256 = { version = "0.13", default-features = false, features = ["ecdsa"] }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
