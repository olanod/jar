name: "CI: Spec"

on:
  push:
    branches: [master]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install elan
        run: |
          curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> $GITHUB_PATH

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Lake packages
        uses: actions/cache@v4
        with:
          path: spec/.lake
          key: lake-${{ hashFiles('spec/lean-toolchain', 'spec/lake-manifest.json', 'spec/lakefile.lean') }}
          restore-keys: lake-

      - name: Cache crypto-ffi build
        uses: actions/cache@v4
        with:
          path: spec/crypto-ffi/target
          key: crypto-ffi-${{ hashFiles('spec/crypto-ffi/Cargo.toml', 'spec/crypto-ffi/Cargo.lock', 'spec/crypto-ffi/src/**') }}
          restore-keys: crypto-ffi-

      - name: Build crypto-ffi
        working-directory: spec/crypto-ffi
        run: cargo build --release

      - name: Run tests
        working-directory: spec
        run: make test

  genesis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # full history needed for genesis replay

      - name: Install elan
        run: |
          curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> $GITHUB_PATH

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Cache Lake packages
        uses: actions/cache@v4
        with:
          path: spec/.lake
          key: lake-genesis-${{ hashFiles('spec/lean-toolchain', 'spec/lake-manifest.json', 'spec/lakefile.lean') }}
          restore-keys: lake-genesis-

      - name: Build tools
        run: |
          cd spec && lake build genesis_select_targets genesis_evaluate genesis_check_merge genesis_finalize genesis_validate genesis_ranking
          cargo build -p jar-genesis

      - name: Smoke test CLI tools
        working-directory: spec
        run: |
          echo '{"prId":1,"prCreatedAt":9999999999,"indices":[],"ranking":[],"variances":[]}' | .lake/build/bin/genesis_select_targets | jq -e '.targets == []'
          echo '{"indices":[]}' | .lake/build/bin/genesis_finalize | jq -e '.weights[0].id == "sorpaas"'
          echo '{"reviews":[],"metaReviews":[],"indices":[]}' | .lake/build/bin/genesis_check_merge | jq -e '.ready == false'

      - name: Verify genesis history
        run: cargo run -p jar-genesis -- replay --mode verify
