[package]
name = "pixels-authorizer"
version = "0.1.0"
edition = "2021"

[dependencies]
ed25519-compact = { version = "2", default-features = false }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_env, values("javm", "polkavm"))'] }
